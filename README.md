
  # Account Selection Login Screen

  This is a code bundle for Account Selection Login Screen. The original project is available at https://www.figma.com/design/7WCtV8rv50EnE0Ba2wLSKo/Account-Selection-Login-Screen.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  